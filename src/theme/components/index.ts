import { ActionIconTheme } from "@/theme/components/action-icon";
import { ButtonTheme } from "./button";

export const components = {
  Button: ButtonTheme,
  ActionIcon: ActionIconTheme,
  // Add more here...
};
